- name: >-
    Download and expand Go {{ go_version }} archive
  unarchive:
    src: "{{ go_src_url }}"
    dest: "{{ go_dest }}"
    remote_src: true
    
- name: >-
    Add {{ go_bin_path }} to $PATH in {{ go_profile_path }}
  lineinfile:
    dest: "{{ go_profile_path }}"
    line: >-
      export PATH="$PATH:{{ go_bin_path }}"
    create: true
