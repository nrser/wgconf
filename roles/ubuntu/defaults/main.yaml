# Global Default Variables
# ============================================================================

# Make sure we have a value for how often we update the apt cache, used when
# installing packages.
apt_cache_valid_time: 86400 # in seconds -> 1 day

reboot_timeout: 600 # in seconds -> 10 mins
