`wireguard-install` Ansible Role
==============================================================================

Used to install Wireguard. That's it. And it only works on Ubuntu. Maybe on
Debian too, IDK.
